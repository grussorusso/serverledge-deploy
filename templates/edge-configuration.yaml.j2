---
api.port: "{{ edge_api_port }}"
etcd.address: "{{ groups['etcd'][0] }}:2379"

# offloading parameters
cloud.server.url: "http://{{ groups['cloud'][0] }}:{{ cloud_api_port }}"
offloading: true
scheduler.policy: cloudonly
