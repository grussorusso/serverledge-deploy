[Unit]
Description=Simple systemd service to run Serverledge.

[Service]
Type=simple
WorkingDirectory={{ app_dir }}
ExecStart={{ app_dir }}/serverledge {{ app_dir }}/conf.yml
ExecStop=/bin/kill -TERM $MAINPID

[Install]
WantedBy=multi-user.target
